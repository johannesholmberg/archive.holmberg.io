require "rubygems"
require "tmpdir"
require "bundler/setup"
require "jekyll"


############################
# Custom in ./tasks/rake/  #
############################
Dir.glob('tasks/rake/*.rake').each { |r| load r}
